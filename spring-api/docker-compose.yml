version: '3.8'
services:
  app:
    build: .
    ports:
      - "8081:8080"
    environment:
      # 'host.docker.internal' apunta a tu máquina local (host) desde dentro del contenedor
      - SPRING_DATASOURCE_URL=jdbc:mysql://host.docker.internal:3306/minerva?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=Jdmm123.
      - SPRING_JPA_HIBERNATE_DDL_AUTO=update
